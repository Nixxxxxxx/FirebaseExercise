<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="german.teach.learn.zero.learnandteachgerman">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GoogleSignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ExerciseSelection">
        </activity>
        <activity
            android:name=".exercises.Exercise1Activity"
            android:label="@string/title_activity_exercise1"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".exercises.CreateExercise1Activity">
        </activity>
        <activity android:name=".ExercisePagerActivity"/>
    </application>

</manifest>